<% if show_minimized?(@file) %>
  close_menu()
<% else %>
  open_menu()
<% end %>

$("#editor_flash").html("<%= escape_javascript(render('shared/flash', :flash => flash)) %>")
$("#editor_errors").html("<%= escape_javascript(render('shared/error_messages', :object => @file)) %>")